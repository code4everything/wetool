{
    "initialize": {
        "width": 1000,
        "height": 700,
        "tabs": {
            "load": [
                "JsonParser",
                "FileManager",
                "RandomGenerator"
            ],
            "support": [
                "FileManager",
                "JsonParser",
                "RandomGenerator"
            ]
        }
    },
    "fileFilter": {
        "regex": "^[^.].*$",
        "showTip": true,
        "description": "只获取匹配的文件，当文件不匹配时根据 `showTip` 设置是否显示提示"
    }
}
